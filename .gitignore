.env
*.env
*.pem
*.key
*.crt
*.p12
*.json.credentials
service-account*.json
secrets/
__pycache__/
*.pyc
.pytest_cache/
.venv/
graphify-out/
data/import/*
!data/import/.gitkeep

# Evaluation dataset: keep the synthetic .example templates, ignore real client content
data/eval/*.yaml
!data/eval/*.example.yaml
data/eval/*.yml
!data/eval/*.example.yml
# Auto-generated review file
REVIEW.md

neo4j-data/
open-webui-data/
postgres-data/
redis-data/

# Local Claude Code settings (permission allowlists, absolute paths) and their
# crash/interrupt temp files — must never be committed
.claude/
.continues-handoff.md

# Personal / client reference material - not part of the shared repo
client-chat.txt
chat-client.md
output/
tmp/
private/

# The entire docs/ tree is kept local-only and untracked (git rm --cached).
# Files remain on disk but are not committed. Note: AGENTS.md references docs as
# required reading, so those pointers dangle for a fresh clone by design.
docs/
