# Daily Report — 2026-07-07

Covers the working session from the evening of 2026-07-06 through 2026-07-07.

## What changed

- **Documentation restructure** (`29b1c4e`): added `CLAUDE.md` (three
  invariants + pointers), tiered the AGENTS.md required-reading list down to
  3 always-read files, declared `AGENT_PROJECT_BRIEF.md` the source of truth,
  archived the completed Phase 1 plan, added the Phase 3 ontology-as-code task.
- **Evaluation fixture scaffold** (`29b1c4e`): `data/eval/` with synthetic
  `*.example.yaml` templates for users/questions/refusals — including the
  graph-path leak case — with real client files gitignored.
- **Review gate overhaul** (`b8d2e91`, `7bbc4b7`, `212f521`): Claude Code CLI
  as primary Stage 2 engine with agy fallback; fail-closed handling throughout
  (partial output discarded on non-zero exit, verdict parsed only from the
  Verdict section, unparseable verdict blocks under `REVIEW_AI_BLOCK=1`);
  prompt fed via stdin; `--disallowedTools` so the reviewer cannot run tools;
  timeouts with kill-after; stderr captured and sanitized into REVIEW.md.
- **Phase 2 Drive metadata sync foundation** (`50065a6`, via Codex when the
  Claude limit ran out): `integrations` app with `DriveConnection`,
  `SourceDocument` (hard default `retrieval_eligible=False`),
  `DrivePermissionSnapshot` (permission version hashing),
  `DriveSyncRun`; metadata sync with controlled exclusions for public-link
  and domain-visible files; 11 mocked tests.
- **Review-finding fixes** (`59095f6`, `c8e5c38`): startup validation for
  `GOOGLE_DRIVE_SCOPE_TYPE`; deterministic test DB via
  `config/settings_test.py` (replaced argv sniffing and a dead conftest);
  `error_summary` stores exception class only (messages can embed Drive
  content).
- **Daily-report journal** (`6d544bd`): this file, plus the end-of-session
  rule in AGENTS.md and CLAUDE.md.
- **Test-settings hardening + gate truncation note** (`425ff12`): Celery
  eager mode in test settings; the hook now tells the AI reviewer when the
  diff was truncated at 40KB (byte-counted) so complete files stop being
  flagged as truncated.

## Decisions

- The review gate fails closed everywhere; AI-verdict blocking stays opt-in
  (`REVIEW_AI_BLOCK=1`) because the AI stage is nondeterministic.
- pytest is the supported test runner; `manage.py test` requires
  `--settings=config.settings_test`.
- Exception messages are never persisted in sync run rows — class name only.

## Next steps

1. Real Google Drive API client (needs the client's service-account
   credentials), folder scanning, content export, API endpoints, Celery task.
2. Send Drake the evaluation-dataset request
   (`private/client-messages/2026-07-05-foundation-live-and-dataset-ask.md`).
3. When the OpenRouter API key exists: verify the `anthropic/claude-sonnet-5`
   slug and add an HTTP-status check in `senior-review.yml`.
4. Backlog: bats smoke tests for the review gate; shared `scripts/lib` for the
   duplicated engine logic; Celery-eager already set in test settings.
