# Daily Report — 2026-07-09

Review-and-fix session over the previous day's Drive-integration commits
(`2a835df..758aa4b`). A code review found the work strong overall (fail-closed
permission logic, per-file ACL isolation, and the `error_summary` data-leak
defense are all correct); this session cleaned up four residual findings.

## What changed

- **A — delegated-subject env fallback removed** (`drive/google_client.py`):
  `build_drive_service` resolved the delegation subject as
  `connection.delegated_subject_email or settings.GOOGLE_DRIVE_DELEGATED_SUBJECT`.
  Clearing the subject via the admin endpoint reported `""` and invalidated
  documents while the built service *still* delegated to the env subject — an
  auth-identity mismatch. The connection field is now authoritative (it is
  already seeded from the env var at bootstrap), so clearing genuinely clears.
- **C — GET Drive endpoints no longer mutate** (`integrations/views.py`):
  `_active_or_bootstrap_connection()` created a `DriveConnection` row on read.
  Root discovery (`GET .../drive/roots/`) now answers from a transient
  (unsaved) connection built from settings; the permission-check diagnostic
  (`GET .../drive/permissions/check/`) returns 409 when no connection exists.
  Create-on-write is retained only for the POST selection/subject endpoints.
- **B — distinct `listing_failed` status** (`integrations/views.py`): the
  permission-check summary previously reported `blocked` both when ACL samples
  were unreadable and when folder listing itself failed. A `listing_failed`
  label now separates "couldn't walk the tree" from "ACLs unreadable" so the
  summary alone points at the right remediation (`folder_listing_errors`
  already carried the count).
- **D — shared BFS folder walk** (`drive/google_client.py`): `list_files` and
  the permission diagnostic duplicated the breadth-first queue + dedup
  scaffolding. Extracted `_walk_files(...)` with an `on_listing_error` hook so
  the two intentional error semantics stay explicit — content sync propagates a
  folder-listing failure (fails closed), the diagnostic counts it and
  continues. Behavior-preserving; no extra Drive API call on the diagnostic
  path (root path is passed in, not re-resolved).

Tests 85 → 88 (backend suite): added `build_drive_service` subject tests (A),
non-mutating GET tests for both read endpoints (C), and re-pointed the
folder-listing status test to `listing_failed` (B). `make lint` clean.

## Decisions

- B and D were, on inspection, *deliberate, tested* choices in the original
  code rather than defects — B's `blocked`-for-listing-failure was locked in by
  a test, and D's duplication was low-risk because both walks already share the
  `_list_children`/`_list_permissions` primitives. They were still tightened as
  requested, but neither was a correctness bug. A (auth mismatch) and C (read
  side-effect) were the substantive fixes.

## Next steps

- Unchanged from 2026-07-08: live-validate Drive ingestion once the pilot
  folder is shared with the service account (content-export + real
  sharing-metadata capture are still offline-test-only; needs a live file whose
  `permissions.list()` succeeds, most likely via domain-wide delegation in a
  real Workspace).
- Phase 3 kickoff: wire real extraction into `queue_document_extraction`, Neo4j
  graph with provenance, everything keyed by connection id.
- Backlog: stale-RUNNING run sweep; per-client service-account provisioning
  script; OpenRouter key checks when the key exists.

## Phase 1 & 2 full verification (this session, later)

Requested independent verification of everything claimed done in Phase 1 and
Phase 2 — not just re-reading the trackers, but re-running and re-checking
against the live stack and real Drive API.

**Phase 1 (all confirmed):**
- `make config`, `make lint`, `make django-check`, full `make test` (88/88,
  across `core`/`health`/`integrations`) all pass clean.
- Infra invariants re-checked directly in the compose files: SpiceDB pinned to
  `v1.51.1` (not `latest`); `kg-private` network is `internal: true` with
  `django`/`celery-worker` also attached to a separate `kg-egress` network;
  Docker healthcheck hits the real `/api/health/` HTTP endpoint.
- Runtime re-verified live: rebuilt containers (found running containers were
  ~7h stale, from before this session's fix commit — rebuilt via `make up`),
  `make health` → all deps ok, `make migrate` → no-op (fully applied),
  `make smoke` → Celery task received and succeeded end-to-end (confirmed in
  worker logs, not just "queued").

**Phase 2 (all confirmed, including a real live Drive round-trip):**
- Models: `retrieval_eligible` defaults `False`; `(connection, drive_file_id)`
  uniquely constrained; `source_permissions_version` is a genuine 64-char
  SHA-256 hex digest; no serializer/admin exposes `raw_permissions` or
  document `content` (the two fields flagged `SECURITY` in the model); no
  SpiceDB/permission logic leaked into Phase 1/2 code (Phase 4 is untouched).
- Live endpoint tests via Django's real test client against the rebuilt
  container (real DB, real auth, real Drive API — not mocked): anonymous → 403,
  non-admin → 403, admin root-list → 200 with the real `kg-graph` folder found
  via the configured service account. Admin permission-check diagnostic on the
  real pilot folder returned `permission_metadata_access: "blocked"` — a live,
  present-day reproduction of the ADR-009 finding (`permissions.list()` still
  `403 insufficientFilePermissions` under folder-only sharing).
  Triggered a real sync via `POST /api/ingest/drive/sync/` with an
  attacker-controlled body (`shared_drive`/bogus IDs) — the audit record and
  actual sync used only the server-side connection scope, confirming the
  anti-widening guarantee live. Run succeeded; the one real file was correctly
  excluded (`retrieval_eligible=False`,
  `exclusion_reason=permission_metadata_incomplete`, empty stored
  `raw_permissions` — not fabricated). Celery worker logs contained only the
  library's own `403 insufficientFilePermissions` reason string — no file
  content, permission payload, or credential material.

No discrepancies found between what the phase trackers/ADRs claim and what the
code and running stack actually do. One process note, not a code defect: the
long-running dev stack does not auto-rebuild on commit, so containers can run
stale code for hours after a push — worth remembering before treating a "live"
check against an already-running stack as current.

## Phase 2 hardening + Phase 3 kickoff (this session, later still)

First use of branches in this repo. `phase-2/hardening` (merged to `main`,
pushed): stale-`RUNNING` `DriveSyncRun` sweep (Celery Beat task every 15 min,
timeout `DRIVE_SYNC_STALE_RUN_TIMEOUT_MINUTES`, default 120) and
`scripts/provision-drive-service-account.sh` (per-client SA per ADR-009;
`secrets/` gitignored). Then `phase-3/graph-foundation` — **local only, not
pushed, per explicit decision**:

- New `graph` Django app: thread-safe lazy Neo4j driver (`graph/db.py`),
  idempotent `manage.py graph_setup` constraints command, ontology as code
  (`graph/ontology.py`, mirrors brief §8 exactly), engine-agnostic
  `ExtractionAdapter` boundary with deterministic `ParagraphChunkExtractor`
  baseline, fail-closed Document/Chunk writers (`MissingProvenanceError`,
  `DocumentNodeMissingError` — nothing writes without provenance, nothing
  drops silently), provenance retrieval guard (`graph/guard.py`).
- `queue_document_extraction` is no longer a stub: `graph/pipeline.py` runs
  stored text/* content → paragraph chunks → Neo4j with provenance;
  unsupported/missing/undecodable content skips with distinct statuses and
  no byte leakage.
- Mid-work review fixes before committing: driver singleton race (lock),
  missing `close_driver()`, Document node keyed `source_document_id` (was
  `document_id`) so identity and provenance can't drift, silent MATCH+CREATE
  no-op replaced with an explicit existence check.
- Suite 91 → 120 tests; lint clean; both commits passed `make review-staged`
  with 0 warnings.

### Decisions

- Extraction runs regardless of `retrieval_eligible`: enforcement lives at
  retrieval (SpiceDB + provenance guard), and Phase 2 only re-queues
  extraction on content change — skipping ineligible docs would strand them
  permanently once permissions become readable. Recorded in the Phase 3
  tracker.
- Phase 2's remaining open items are client-ops, not code: full sharing
  metadata + live content-export validation both wait on domain-wide
  delegation in a real Workspace (ADR-009). Creator metadata stays deferred
  (Revisions API).

### Live validation (same session)

Rebuilt the stack and validated the whole Phase 3 path against the real
services: `graph_setup` applied both constraints to live Neo4j (verified via
`SHOW CONSTRAINTS`); a synthetic document ran through the **real Celery
worker** end-to-end (`queue_document_extraction.delay` → `extracted`,
2 chunks) — Document + Chunk nodes landed in Neo4j with full provenance
(`source_document_id`, `connection_id`, `drive_file_id`,
`source_permissions_version`) and `belongs_to` edges; worker logs carried
ids/status/counts only, zero content; smoke data then deleted from both
Postgres and Neo4j (verified 0 remaining).

### Next steps

- Evaluate `neo4j-graphrag` / Graphify / Graphiti behind the adapter; record
  the fact-level vs document-level provenance finding in `04-decisions.md`.
- Entity/relationship writers, then vector index.
- Ask Drake about domain-wide delegation for the pilot Workspace.
