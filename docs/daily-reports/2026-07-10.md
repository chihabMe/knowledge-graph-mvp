# Daily Report — 2026-07-10

## What changed

- Corrected ADR-010: LLM-backed extraction was live smoke-validated on
  2026-07-09. A production OpenRouter key/model configuration is still pending;
  the deterministic paragraph extractor remains the default.
- Completed the requested report-only review of the Phase 3 branch. No Phase 3
  code was changed in this session.

## Decisions

- Keep the local ignored `.env` on the chosen development LLM endpoint for
  development with non-sensitive test documents. The user will explicitly
  request a production OpenRouter change before any client-data sync.
- Do not push or start Phase 4 in this session.
- The review confirmed two correctness follow-ups that need user approval
  before implementation: make a document replacement one Neo4j transaction,
  and give extraction work retry/recovery handling. It also identified a
  requirements discrepancy: the graph stores a narrower provenance shape than
  the canonical brief specifies. Relationship chunk validation is future-adapter
  hardening, not a demonstrated defect in the selected GraphRAG adapter.

## Next steps

- Decide whether to apply the two confirmed Phase 3 fixes, then add their
  regression tests and revalidate the extraction path. Resolve or explicitly
  revise the provenance-contract discrepancy before Phase 5 retrieval.
- Start Phase 4 SpiceDB permission sync only after the Phase 3 follow-ups are
  resolved or explicitly deferred.
