# Daily Report — 2026-07-11

## What changed

- Committed the Phase 3 hardening work as `b22f532` (content-version safety
  plus the review fixes: stale-RUNNING sweeper, bounded cross-sync retries,
  PENDING requeue gate, typed relationship endpoints, engine-owned retryable
  exceptions, UNWIND-batched writers, concurrent chunk extraction, guard
  alias validation). Pre-commit hooks green: 161 tests, Ruff, secret scan.
- Rebuilt the running stack (`make up`) and applied migrations 0004 + 0005 —
  the worker now runs the committed code and schema.
- **ADR-011:** resolved the provenance-contract discrepancy on the docs side
  (user decision). Brief section 7 now specifies the implemented contract —
  identity triple + `source_permissions_version` on every node, relationship,
  and chunk; chunk attribution via `mentions` edges / `chunk_index`;
  `Document.content_hash` as the content version. `extraction_run_id`,
  `confidence`, and per-element timestamps are explicitly deferred audit
  metadata. Updated: `AGENT_PROJECT_BRIEF.md`,
  `ai-context/04-decisions.md`, `ai-context/07-ai-coding-security-rules.md`,
  and the Phase 3 tracker.
- Merged the completed Phase 3 branch into `main`, then added deployment
  hardening there: production-only Django HTTPS/cookie/secret checks, explicit
  logging and persistent database connections, removal of the unused admin,
  a pinned multi-stage uv image, production resource/log/health policies, and
  a thin development Compose overlay with `make up`/`make up-prod` commands.
  Local validation is green (161 tests, Ruff, Django checks, and resolved
  Compose configs); the fresh image build is pending because GHCR timed out
  twice while fetching the pinned uv image.

## Decisions

- ADR-011 (above): docs follow the implementation because ADR-010's
  per-document entity scoping makes the brief's plural `source_documents`
  shape structurally impossible, and a fabricated `confidence` value would be
  worse than none.
- Do not start Phase 4 yet — explicit user instruction this session.
- Keep one multi-stage Dockerfile and layer a small development Compose
  override over the shared infrastructure/application definitions; avoid
  parallel dev/prod copies that can drift.

## Next steps

- Phase 4 (SpiceDB permission sync) is unblocked and next when the user says
  go.
- Operational note: the sweepers (`sweep_stale_drive_sync_runs`,
  `sweep_stale_graph_extractions`) run under celery-beat, which only starts
  with the `scheduler` profile (`make up-all`); plain `make up` does not run
  them.
- Still pending from earlier: production OpenRouter key/model configuration
  before any client-data sync.
- Retry the production image build when GHCR is reachable, then set a strong
  production `DJANGO_SECRET_KEY` and non-default ops credential before running
  `make up-prod`.

## Phase 4 work session

### What changed

- Implemented the Phase 4 SpiceDB schema/client lifecycle, permission-only
  Drive folder/document scans, nested Directory group resolution, exact tuple
  reconciliation and verification, durable Celery/admin sync flow, health
  check, and fully consistent allowed-document lookup.
- Added migration 0006 and permission-safety coverage; 181 backend tests pass,
  Ruff and Django checks pass, and all Compose configurations resolve.
- Live local SpiceDB smoke passed direct, nested-group, multi-level folder,
  unrelated-user denial, and consistent-read revocation behavior.

### Decisions

- Use `kgm/` as the schema namespace because SpiceDB v1.51 rejects the planned
  two-character `kg/` namespace segment; authorization semantics are unchanged.
- Phase 4 is code complete, with delegated Google Workspace ACL and Directory
  group validation explicitly pending client credentials.

### Next steps

- Begin Phase 5 by composing `allowed_source_document_ids()` with the Neo4j
  provenance guard; an empty allowlist must always deny all retrieval.
- Validate real Drive inheritance and nested groups under domain-wide
  delegation when client Workspace credentials are available.
