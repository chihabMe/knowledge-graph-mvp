# Daily Report — 2026-07-13

## What changed

- Added a configurable maximum age for successful SpiceDB verification
  evidence. Expired documents now fail closed in the shared permission
  predicate even when SpiceDB still returns an old grant.
- Added regression coverage for queryset, instance, and allowed-document
  lookup behavior; the full suite passes with 206 tests.
- Updated the canonical brief, ADR/security/phase trackers, environment
  example, and operator guide with the freshness contract.
- Added a development-only personal Google OAuth path with mode-gated settings,
  owner-only token persistence, Docker secret mounts, and owned-folder discovery.
- Completed a live `kg-graph` Drive sync for one PDF, added safe PDF text
  extraction, and fixed the PostgreSQL `SELECT FOR UPDATE` nullable-join bug
  found by the live GraphRAG run.
- Verified the real document through Postgres, OpenRouter, Neo4j, and SpiceDB:
  2 chunks, 12 entities, 27 relationships, and zero missing provenance fields.

## Decisions

- `PERMISSION_SYNC_INTERVAL_SECONDS` remains the healthy refresh cadence
  (default 900 seconds); `PERMISSION_VERIFICATION_MAX_AGE_SECONDS` is the hard
  failure bound (default 1800 seconds) and must be greater than the cadence.
- This is Phase 4 authorization hardening. Phase 5 remains not started.
- Personal OAuth is a development workaround only. Client production remains
  service-account/Workspace controlled; nested groups and delegated Workspace
  behavior are not proven by this run.
- Chunk embeddings remain intentionally absent because the current embedding
  adapter is a no-op; live entity and relationship extraction is operational.

## Next steps

- Start Phase 5 with the authenticated query contract and compose
  `allowed_source_document_ids()` with the Neo4j provenance guard before any
  context assembly or LLM call.
- Complete live delegated Workspace ACL and nested-group validation when client
  credentials become available.
- Implement and validate the production embedding adapter before vector
  retrieval work, then repeat acceptance with multiple file types and ACLs.

## Session closeout

### What changed

- Reconfigured the local graph extractor through OpenRouter and reran the real
  Drive PDF end to end. The final graph contains 1 document, 2 chunks, 19
  entities, and 44 total relationships; no relationships were skipped and no
  node or relationship is missing required provenance.
- Re-ran the complete backend suite: 212 tests passed. Docker Compose
  configuration validation also passed.
- Produced a three-page Milestone 1 completion report with the initial and
  final Neo4j screenshots at
  `output/pdf/milestone-1-completion-report.pdf`; the client was notified that
  the live demonstration used development OAuth while Workspace remains the
  production path.

### Decisions

- Milestone 1 is complete for development acceptance: Foundation, Ontology,
  and live Drive-to-Neo4j graph construction are demonstrated on real data.
- Model selection remains environment-configured behind OpenRouter rather than
  hard-coded into the architecture or client report.
- Personal OAuth remains a development workaround. Client production still
  requires Workspace-controlled credentials and delegated ACL/group
  validation when Drake's environment is ready.

### Next steps

- Start Phase 5 in the next session with the authenticated `/api/query/`
  contract and trusted user identity.
- Compose `allowed_source_document_ids()` and the Neo4j provenance guard into
  every retrieval path before assembling context or calling an LLM; add leak
  and refusal tests with that first vertical slice.
- Add production embedding/vector retrieval and enforce OpenRouter privacy and
  parameter-routing controls before processing client production data.

## Phase 5 permission-safe retrieval slice

### What changed

- Added the authenticated, question-only `/api/query/` contract; request-body
  identity fields are rejected and the server-side Django session email is the
  only authorization identity.
- Added SpiceDB-first orchestration, fresh permission-evidence rechecking,
  provenance-guarded Neo4j chunk and bounded one-hop fact retrieval, extractive
  answers, permitted citations, and a shared fail-closed refusal.
- Added focused API/unit/leak coverage for restricted users and connected
  facts, empty allowlists, stale or invalid evidence, missing provenance,
  SpiceDB/retrieval failures, unauthenticated access, and identity spoofing.
- Validation passed: 23 focused retrieval tests, 235 complete backend tests,
  Ruff lint/format, Django system checks, and all Compose configurations.
- Mounted the new `retrieval` package in the development Compose overlay,
  restored Django health, and verified that an unauthenticated live query
  returns the expected HTTP 403 response instead of resetting the connection.
- Replaced the development OAuth token's single-file bind mount with a
  dedicated owner-only writable directory so refresh remains atomic while the
  client secret and service-account key stay read-only. Manual and scheduled
  permission-sync runs then succeeded with one document verified.
- Tightened baseline keyword relevance by dropping generic question words and
  requiring two meaningful matches for multi-term questions. Live testing now
  refuses the unrelated Atlas question and returns permitted PDF citations for
  a relevant knowledge-graph question.

### Decisions

- This slice makes no OpenRouter call and does not claim vector or hybrid
  retrieval while `NoOpEmbeddingAdapter` remains active.
- Django session authentication is the current server-trusted backend boundary;
  Google OIDC/Open WebUI identity provisioning remains Phase 6 work.
- Development OAuth remains a local validation workaround; production still
  uses the service-account/Workspace path and awaits delegated ACL/group proof.

### Next steps

- Implement the production embedding adapter, then add a Neo4j vector query
  that composes the same SpiceDB allowlist and provenance guard before any
  OpenRouter answer synthesis.
