# Daily Report — 2026-07-15

## What changed

- Completed ADR-015 WP1 locally: fail-closed configuration, dedicated versioned token encryption, additive authorization/evidence models and migration, and read-only secret mounts.
- Completed ADR-015 WP2 locally: session-bound Drive OAuth connect, callback, status, reconnect, disconnect, safe result page, throttling, claim/domain/scope enforcement, encrypted refresh-token handling, and generation invalidation.
- Completed ADR-015 WP3 locally: additive direct `oauth_viewer` schema, one-user scoped read/reconcile/delete helpers, causal exact verification, and delegated-mode isolation.
- Implemented and fake-validated the WP4 indexed-ID-only user Drive metadata adapter; real-Google smoke remains pending.
- Passed six focused WP4 tests and the complete 384-test backend suite, plus Ruff, formatting, migration drift, Django, whitespace, and official Authzed schema/assertion validation.
- Completed WP5 locally with durable per-authorization visibility runs, pre-invalidation, direct-tuple reconciliation, causal evidence commits, bounded Celery locking/retries, scheduling, manual refresh, and stale-run recovery.
- Completed WP6 locally with mutually exclusive mode-aware retrieval, fully consistent direct `oauth_viewer` reads, exact authorization/generation checks, fresh `UserDocumentVisibility` intersection, and a post-Neo4j evidence recheck.
- Passed 12 new WP6 security tests and the complete 408-test backend suite, plus full Ruff/formatting, Django, migration drift, and production/development Compose rendering.
- Added the missing development bind mount for `authorization/`; recreated only Django and Celery, then confirmed Django/all backing-service health, Open WebUI health, and a live Celery ping.

## Decisions

- WP2 creates no document grant and queues no visibility work; direct authorization begins only with WP3's distinct SpiceDB relation.
- A reconnect without a new Google refresh token is accepted only when the existing ciphertext remains decryptable; broader Drive scopes are rejected.
- Direct per-user relationships use opaque existing document/user IDs and never share a reconciliation read or delete scope with another user.
- The user visibility adapter accepts no file IDs and exposes no Drive listing, export, or download operation; it loads active indexed candidates from PostgreSQL.
- Per-user retrieval never evaluates SpiceDB's combined `view` permission; delegated tuples, per-user tuples, and PostgreSQL evidence are never unioned or used as fallbacks.
- Workspace payment review blocks only real-Google validation; fake-backed implementation and local infrastructure validation can continue safely.

## Next steps

- Start WP7 with the real Open WebUI Google login/onboarding configuration and recovery-safe login lockdown once the Workspace account is approved; then run WP4's real Drive smoke and WP8 two-user acceptance.
