# Daily Report — 2026-07-17

## What changed

- Fast-forwarded the active Phase 6 branch to the reviewed ADR-015 implementation; preserved the prior uncommitted Open WebUI slice in `stash@{0}`.
- Added per-user-mode ingestion that does not require service-account ACL reads and uses connection-generation provenance.
- Made successful graph extraction the coarse global content-ready gate in per-user mode.
- Added deny-first `switch_drive_permission_authority` cleanup and best-effort direct-tuple deletion on OAuth disconnect.
- Updated canonical status, phase trackers, operator configuration, and the local implementation report.
- Passed the complete 416-test backend suite, Ruff, formatting, migration drift, Django checks, and all Compose renderings.
- Added a standalone Phase 6 per-user OAuth cutover work report for client and developer handoff.
- Prepared the completed Phase 5 commit range for a protected-branch GitHub merge, excluding all Phase 6 planning and implementation work.

## Decisions

- `delegated_acl` remains the safe configuration default until an operator explicitly configures OAuth and runs the cutover command.
- Delegated mode retains its existing ACL-read failure behavior; only `per_user_oauth` separates content readiness from ACL metadata.
- A failed SpiceDB cutover cleanup leaves the Drive connection disabled.

## Next steps

- Configure the customer-controlled OAuth clients and secrets, then run the real indexed-ID Drive smoke.
- Run real Open WebUI Google login and two-user allowed/restricted, revocation, expiry, and SpiceDB-failure acceptance.
