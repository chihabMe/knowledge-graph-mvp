# Daily Report — 2026-07-18

## What changed

- Reconciled the complete reviewed Phase 6 OAuth, Open WebUI, model, task, retrieval, settings, URL, and Compose integration into the active branch.
- Installed the two Google OAuth client files under a private mode-0700 directory and configured the ignored local `.env` without exposing secret values.
- Generated independent user-token encryption, Open WebUI service, identity-JWT, and WebUI session secrets.
- Removed active references to the obsolete Drake TV service account and legacy developer OAuth credentials.
- Updated the canonical and working project documentation to the reviewed ADR-015 per-user OAuth architecture.
- Added fail-closed keyless ADC support for local impersonation and attached-service-account deployment, including Compose secret mounting and metadata-server fallback.
- Installed Google Cloud CLI 576.0.0 under the operator's local profile and granted the controlled pilot administrator Token Creator on only the ingestion service account.
- Created a mode-0600 local impersonated ADC credential outside the repository after Google's ADC browser page returned a generic error; no private service-account key was created.
- Selected the exact renamed `Knowledge Graph Pilot` root, switched the connection to per-user authority, and re-ingested all three documents with content and graph extraction while preserving deny-by-default retrieval.
- Added a Google OIDC Django-session bootstrap that reuses the identity-only Open WebUI login client, verifies one-time state, nonce, PKCE, issuer, audience, verified email, and hosted domain, creates unusable-password users, and stores no Google token.
- Added PKCE to the separate Drive authorization flow, exchanged only the authorization code after callback-state validation, and redacted callback query strings from local Django request logs.
- Marked successfully graph-extracted per-user-mode documents content-ready without using that coarse gate as an authorization grant; final retrieval still requires a direct SpiceDB tuple and fresh matching user evidence.
- Completed identity bootstrap, Drive consent, indexed-ID visibility synchronization, and final allowlist validation for both pilot users.
- Updated a successful Drive OAuth callback to queue the existing bounded
  visibility refresh immediately for only the verified Django identity, while
  keeping consent successful and the durable scheduler available if dispatch
  is temporarily unavailable.
- Updated the canonical brief, Phase 6 tracker, active completion plan,
  historical implementation record, and ignored detailed report with the live
  reconnect evidence.
- Resolved the tracked over-32-message follow-up: the Open WebUI chat adapter
  now normalizes invalid chat-completion payloads (oversized message lists and
  other serializer failures) into a controlled OpenAI-compatible error body
  instead of returning raw DRF serializer errors, without reflecting submitted
  content or reaching `answer_query()`. Added leak-focused tests confirming
  malformed and oversized payloads never reach the permission/query service
  and never expose DRF internals.
- Independent full Phase 6 review closeout fixes (four review findings, none
  a confirmed leak): the Drive disconnect flow now logs a redacted warning
  (exception class only) instead of silently swallowing a failed SpiceDB
  oauth_viewer cleanup; the delegated permission sync now enforces authority
  isolation structurally by failing closed if its desired state ever contained
  an oauth_viewer tuple and by removing plus error-logging any stray
  oauth_viewer tuples it reads; the query-layer provenance guard now rejects
  empty-string provenance in both the generated Cypher and the record check;
  and the literal private canary value recorded in three acceptance documents
  was redacted to comply with the pass/fail-only evidence policy.
- Follow-up refactor: inlined the static validation-error selection directly
  into the authenticated `OpenWebUIChatCompletionsView`, removing a standalone
  module-level helper that the automatic pre-commit review had flagged
  (heuristically) as a new retrieval-adjacent function without a visible
  SpiceDB call. Confirmed this was a static-checker false positive — the
  helper only builds a static error response before any document retrieval —
  and preserved identical behavior with no new SpiceDB call needed.

## Decisions

- Keep the controlled connection on `GOOGLE_PERMISSION_AUTHORITY=per_user_oauth`; no document is retrieval-eligible until fresh employee OAuth evidence exists.
- Keep the OAuth client files and encryption keyring outside the repository; tracked files contain only variable names and mounted container paths.
- Keep the organization policy blocking service-account key creation enabled; local development uses short-lived impersonation and Google Cloud production should attach the service account.
- Treat the ingestion service account as content-only. Its Viewer access can discover and export the shared pilot documents but cannot reliably enumerate their ACLs.
- Treat `SourceDocument.retrieval_eligible` as content readiness in per-user mode, never as proof that any user may retrieve that document.
- Keep Phase 6 formally open until the operator reviews the updated report and
  explicitly approves closeout, even though the core live permission matrix is
  validated.
- Treat the extra permitted citation on a shared-only answer as a relevance
  issue, not an authorization leak; decide during closeout whether it blocks
  the phase or becomes follow-up work.
- Recorded the pre-commit heuristic warning on the validation-error helper as
  reviewed with no security issue: the flagged code never queries Neo4j,
  never calls `answer_query()`, and runs entirely before the
  SpiceDB-protected retrieval path for valid requests. Refactored anyway
  (inlined into the authenticated view) to remove the false-positive trigger
  and keep future automated review signal clean, at the operator's request.

## Validation

- Focused Phase 6 security/configuration suite: 95 tests passed.
- Complete backend regression suite: 413 tests passed.
- Django system check passed with the real local per-user OAuth configuration.
- Migration drift check reported no changes.
- Ruff lint and format checks passed.
- Infrastructure and application Compose rendering passed.
- OAuth client IDs, secrets, callbacks, secret-file paths, keyring structure, domain, adapter keys, and safe authority state passed local consistency checks.
- Keyless ADC token refresh, service-account impersonation, pilot-folder discovery, and a Google Docs content export passed live validation.
- Live `permissions.list()` under the Viewer service account returned Google's expected `403 insufficientFilePermissions`, confirming that per-user OAuth—not the content identity—must supply visibility evidence.
- Complete regression validation after the ADC change: 420 tests passed; migration drift, Django checks, Ruff lint/format, and Compose rendering all passed.
- Session-bootstrap and mode-aware ingestion regression validation: 436 tests passed; Ruff, formatting, migration drift, Django checks, Compose rendering, local startup, and health checks passed.
- Final backend regression validation: 441 tests passed; Ruff and formatting passed, migrations were current, Django reported no issues, and Django/PostgreSQL/Redis/Neo4j/SpiceDB health was green.
- Both live user-visibility runs succeeded. Each considered three indexed documents, verified two as visible, denied one, returned no unknown result, and wrote two direct relationships.
- Final fully consistent SpiceDB plus fresh-evidence lookup returned only each user's own private document and the document visible to both users; the other user's private document was denied in both directions.
- Pinned Open WebUI 0.10.2 started healthy at `http://localhost:3000`, and its live login screen exposed only the expected Google sign-in path.
- User 1 completed real Google login through Open WebUI. Two initial chat requests failed closed because the 1,800-second visibility evidence had expired; no stale context or citation was returned.
- A fresh user 1 visibility run again considered three documents, verified two, denied one, and restored a two-document allowlist. Keyword retrieval returned only `User 1 private document` and `Visible to both users` before the UI retry.
- Isolated repeated safe refusals to answer-model behavior rather than permissions: the configured `z-ai/glm-5.2` alternated between supported and unsupported for the same permitted context, while the documented `openai/gpt-4.1-mini` comparison returned both permitted citations.
- Switched only the ignored local answer-model setting to `openai/gpt-4.1-mini`, recreated Django, and replayed the exact signed Open WebUI identity plus service-bearer request. It returned both permitted source titles, no User 2 source, and no refusal through the live OpenRouter route.
- Switched the ignored local runtime again at the operator's request to OpenRouter's confirmed `deepseek/deepseek-v4-flash` slug. After fresh visibility synchronization, the exact signed-user adapter request succeeded when the question addressed the already-filtered accessible sources, returned both permitted citations, and returned no User 2 source. DeepSeek correctly refuses prompts that ask the model itself to determine authorization; Django and SpiceDB remain the authorization authority.
- User 1's visible Open WebUI chat returned the expected controlled private fact with only the two permitted source titles. User 2 then completed real Open WebUI Google login, but Open WebUI correctly hid the unregistered raw provider model from the non-admin role. Registered the single `client-knowledge-graph` workspace-model override in the local Open WebUI volume and granted read access only to the User 2 pilot identity; a signed User 2 `/api/models` request then returned exactly that model.
- Replaced per-user Open WebUI model onboarding with an environment-authoritative single-model policy: OAuth signup is restricted to `GOOGLE_USER_OAUTH_ALLOWED_DOMAIN`, normal authenticated tenant users automatically discover the Django-owned model, and the built-in arena model is disabled. After recreating Open WebUI, a signed standard-User-2 request returned exactly `client-knowledge-graph` and no other model.
- Diagnosed User 2's first visible chat refusals as the intended stale-evidence denial: its last successful two-visible/one-denied snapshot was about 81 minutes old against the 30-minute maximum, and the requests stopped before embeddings or OpenRouter. Queued a fresh User 2 run, which again checked three documents with two visible, one denied, and zero unknown. Started the existing `scheduler` profile's Celery Beat service so both users are refreshed every 15 minutes before evidence expires.
- Created GitHub issue #2 to track a later privacy-safe Django/PostgreSQL usage ledger for per-user and tenant token counts, embedding usage, provider cost, latency, outcomes, aggregation, retention, and admin reporting without persisting prompts, answers, credentials, or source data.
- Began live access-removal acceptance with the first pilot user. After the administrator removed that user's direct private-document share, a forced visibility run succeeded with one visible shared document, two denied private documents, and zero unknown; the final fully consistent SpiceDB-plus-evidence allowlist contained only the shared source.
- Validated and documented a bounded production target of five-minute visibility refreshes with ten-minute evidence expiry, change-triggered acceleration plus periodic reconciliation, pre-expiry synchronization monitoring, and a client-approved chat-history deletion/retention policy. The running POC remains at 15/30 until monitoring is implemented.
- Completed the live Phase 6 permission matrix: User 1 share removal and
  restoration, User 2 OAuth disconnect and reconnect, evidence expiry,
  DeepSeek routing, and SpiceDB outage/recovery all preserved fail-closed
  retrieval and restored only each user's exact permitted sources.
- Final automated regression for the callback change passed 443 isolated
  tests, Ruff lint and formatting, migration drift, Django checks, and all
  infrastructure/application/development Compose renderings.
- Live-validated the callback-triggered refresh with User 2: disconnect denied
  immediately, reconnect stayed denied until fresh evidence existed, and the
  next successful chat returned only the expected private canary value, the
  User 2 private source, and the shared source without waiting for the
  15-minute scheduler.
- Full backend regression after the validation-error normalization and the
  follow-up inline refactor: 445 tests passed; Ruff lint and format checks
  passed; migration drift check reported no changes.
- Full backend regression after each review-finding fix (disconnect cleanup
  logging, delegated-sync authority isolation, empty-string provenance
  rejection): 446, 448, and 449 tests passed respectively with Ruff lint and
  format clean on every commit; the new tests assert redacted logging, stray
  oauth_viewer removal with fail-closed desired-state protection, and
  empty-provenance rejection.

## Next steps

- Review the ignored detailed Phase 6 report and decide whether the remaining
  citation over-inclusion on shared-document questions is a blocker or
  tracked follow-up work.
- Do not mark Phase 6 complete, push, merge, or open a pull request until the
  operator explicitly approves the closeout.
