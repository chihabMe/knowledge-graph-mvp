# Daily Report — 2026-07-19

## What changed

- Re-ran the complete backend validation in Docker on the Phase 6 branch:
  449 tests passed, Ruff clean, no migration drift.
- The operator reviewed both untracked Phase 6 reports (the local
  implementation report and the per-user OAuth cutover work report; both are
  intentionally gitignored) and approved formal Phase 6 closeout.
- Marked Phase 6 complete in the phase tracker and updated
  `AGENT_PROJECT_BRIEF.md`, `README.md`, and `AGENTS.md` to record closeout
  and make Phase 7 the active phase.
- Committed `docs/phase-7-implementation-plan.md`, the Phase 7 working plan
  written after a full code assessment and vendor-documentation research pass
  (Drive changes-feed semantics, quotas, push-channel constraints).

## Decisions

- Citation over-inclusion (a shared-document question cited an additional
  permitted source) is ruled a non-blocking relevance-quality follow-up, not
  an authorization leak and not a Phase 6 blocker. Tracking issue #6 records
  it; the ruling is recorded in the tracker and brief.
- Phase 7 sequencing per the implementation plan: WP4 content-currency gate
  (issue #5) as warm-up, then WP1 freshness monitoring → WP2 change feed →
  WP3 tighten to the 5/10 target, with WP5 evaluation runner in parallel
  (gated on the operator authoring the real question set). Each work package
  gets its own branch and squash PR onto `main`.
- The Drive change feed remains strictly an accelerator; the periodic
  per-user sweep stays the SLA guarantee (vendor docs do not guarantee
  permission-only changes reach the service account's change log).

## Phase 7 WP4 (same day, branch `codex/phase-7-wp4-content-currency`)

- Implemented the content-currency retrieval gate (issue #5): retrieval
  evidence now carries the graph document's `content_hash` as
  `content_version`, and the post-Neo4j recheck in `retrieval/services.py`
  excludes any chunk or fact whose version does not exactly match the
  document's current PostgreSQL `content_hash`. Empty or missing versions are
  excluded — fail closed — so superseded content cannot reach answer context
  while re-extraction is pending or failed.
- Added tests: stale versions excluded from context and citations, all-stale
  returns the controlled refusal, missing/empty versions fail closed, and
  Neo4j record parsing captures (or blanks) the document content hash.
  Existing fixtures across the retrieval suites now stamp matching versions.
- Full Docker validation: 453 tests passed (449 baseline + 4 new), Ruff and
  format clean, no migration drift.
- Environment note: `uv` (0.11.29) and the backend `.venv` are working again
  locally, so the pre-commit hook can run its full gate; the "broken venv,
  Docker-only validation" note in the Phase 7 plan is stale.

## Next steps

- Push the Phase 6 branch, open the squash PR onto `main`, and merge — each
  step on explicit operator go-ahead.
- Start Phase 7 WP4 on its own branch from the merged `main`.

## Phase 7 WP1 (same day, branch `codex/phase-7-wp1-freshness-monitoring`)

### What changed

- Added a minimal scheduler/worker heartbeat and identity-free freshness
  aggregation for active per-user and delegated permission targets, including
  last-success/evidence age, run/backlog age, duration, failures, denied and
  unknown results, and expired evidence.
- Added the authenticated `/api/health/freshness/` endpoint, non-200 degraded
  signaling, structured count-only logs, Celery beat wiring, strict alert-
  before-expiry settings validation, and the additive heartbeat migration.
- Added aggregation, task, endpoint authentication/redaction, stale-heartbeat,
  delegated-mode, and fail-closed expired-evidence tests; documented the
  environment contract and Uptime Kuma monitor.
- Docker validation passed: 477 tests, Ruff clean, and no migration drift.

### Decisions

- WP1 is required before the 5-minute refresh/10-minute evidence-expiry target:
  expiry already prevents leaks, while the external health poll prevents a
  dead scheduler or worker from causing silent availability loss.
- The freshness bearer is mandatory outside development so the authenticated
  endpoint cannot be deployed without a usable external monitor credential.

### Next steps

- Configure the untracked freshness bearer in the deployment and Uptime Kuma,
  then stop Celery Beat deliberately and verify alert delivery before evidence
  expiry; do not enable the 5/10 target before that live acceptance passes.
- After WP1 live closeout, begin WP2 Drive change-feed polling from the Phase 7
  implementation plan.
