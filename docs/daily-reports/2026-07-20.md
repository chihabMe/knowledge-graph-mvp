# Daily Report — 2026-07-20

## What changed

- Completed Phase 7 WP1 freshness monitoring: scheduler heartbeat persistence,
  aggregation, Celery Beat recording, health/status endpoints, and focused
  tests.
- Validated the isolated WP1 work with Ruff, Django checks, migration drift,
  Compose rendering, and the full 477-test backend suite.
- Implemented ADR-018 guided Open WebUI-to-Drive onboarding on the isolated
  `codex/openwebui-drive-onboarding` branch.
- Added five-state connection readiness, a pre-retrieval chat gate, callback
  status polling with automatic Open WebUI return, and terminal
  `invalid_grant` credential invalidation.
- Added focused state, API, streaming, URL-validation, refresh-failure, and
  fail-closed cleanup tests; 94 focused tests and the full 469-test backend
  suite pass in Docker.
- Rebuilt and recreated Django, Celery worker, and Celery Beat from the isolated
  onboarding worktree; all became healthy while Open WebUI and persistent data
  services remained in place.
- Live first-connect acceptance passed for admin, User 1, and User 2 with the
  exact expected 3/3, 2/3, and 2/3 source visibility and no cross-user leak.
- Added one safe retry for HTTP-success OpenRouter responses that violate the
  strict answer schema, plus focused retry/no-retry tests.
- Corrected the tracked pre-commit Compose check to use the same config-only
  environment fallback as `make config`.
- Guided User 2 disconnect/reconnect passed: access dropped to zero immediately
  after local denial and returned to exactly two visible/one denied documents
  only after renewed consent and callback-triggered synchronization.
- Combined the committed WP1 and onboarding changes in a clean integration
  worktree; the full integrated 493-test backend suite passes.
- Deployed the integrated branch to the development stack. Migration `0010` is
  applied, Django and Celery are healthy, and the live freshness task reports
  `ok` with three current sync targets and no expired/unknown evidence.

## Decisions

- Reuse the existing three OAuth callbacks and `WEBUI_URL`; do not patch Open
  WebUI or add a handoff token/new redirect variable.
- Only explicit structured Google `invalid_grant` requires renewed consent;
  transient failures remain retryable.
- Keep freshness reporting bounded and metadata-only; health responses expose
  controlled timestamps/counts without document, identity, or credential data.
- Combine WP1 and onboarding only through additive conflict resolution in a
  clean integration worktree, preserving both original branches.

## Next steps

- Resume Phase 7 with the next incomplete work package from the implementation
  plan.

## Review follow-up session (same day)

### What changed

- Reviewed the Phase 7 branch (plan, WP4 gate, WP1 monitoring) and fixed all
  eight review findings:
  - Freshness aggregation now uses DB aggregates for backlog and evidence
    expiry and samples only the most recent completed runs
    (`FRESHNESS_RUN_SAMPLE_LIMIT`, default 20); the failure-streak reading
    caps at the sample size, which still drives a warning.
  - New daily `integrations.prune_completed_sync_runs` beat task deletes
    completed sync-run rows older than `SYNC_RUN_RETENTION_DAYS` (default 14),
    always keeping the latest successful run per target because delegated
    last-success is derived from run rows.
  - Never-synced, just-connected targets now report warn instead of error for
    `FRESHNESS_NEVER_SYNCED_GRACE_SECONDS` (defaults to the mode's sync
    interval); retrieval denies them regardless.
  - Data migration 0011 seeds the freshness heartbeat at migrate time so
    fresh deploys start green instead of erroring until the first tick.
  - The freshness report gained `content_refresh_pending_documents`
    (informational) and `content_extraction_failed_documents` (warn), making
    WP4 refusal windows visible to operators.
  - Documented a second Uptime Kuma keyword monitor (`"status":"error"`) as
    the paging channel, keeping the status-code monitor as the softer
    warn channel.
  - Added the live fail-closed drill runbook
    (`docs/runbooks/freshness-drill.md`) gating WP1 closeout and WP3.
  - Documented the expected post-edit refusal window for pilots in
    `docs/google-drive-next.md`.
- Validation in Docker: 490 tests passed (was 477), Ruff lint and format
  clean, no migration drift.

### Next steps

- Run the freshness fail-closed drill live per the runbook, then flip the
  Phase 7 tracker `[~]` items and close WP1.
- These fixes were reconciled onto the integrated WP1 + onboarding branch
  (`codex/phase-7-wp1-integrated-with-fixes`); rerun the full suite there
  before treating either line as canonical.

## Handoff — branch state and live drill blocker (end of session)

Read this section first. It is the complete state needed to resume; nothing
else in this file is required to continue.

### Branch state

- **`codex/phase-7-wp1-integrated-with-fixes`** is the branch to continue
  from. It stacks, in order: WP1 freshness monitoring (`cf59588`) → the Open
  WebUI Drive onboarding work (`3aca96c`) → the dev-deployment record
  (`f17cea1`) → two review-fix commits cherry-picked and reconciled this
  session (`61d5b2a`, `b4f4181`).
- It was validated in Docker only (pytest + ruff + `makemigrations --check`):
  **506 tests passed**, lint clean, no migration drift. It has **not** been
  deployed or live-tested — the earlier "deployed the integrated branch" note
  above refers to the pre-fix commit `f17cea1`, before the eight review fixes
  landed.
- `codex/phase-7-wp1-freshness-monitoring` (the two review-fix commits before
  reconciliation) and `codex/phase-7-wp1-onboarding-integrated` (WP1 +
  onboarding, without the review fixes) both still exist untouched. Treat
  them as superseded by the integrated-with-fixes branch once you confirm it
  looks right; nothing has been deleted.
- Nothing has been pushed to `origin`. All work is local.

### What the eight review fixes did (full detail in the "Review follow-up
session" entries above)

Bounded freshness aggregation queries (DB aggregates instead of loading every
run/evidence row), added daily `prune_completed_sync_runs` retention, added a
never-synced grace period, seeded the scheduler heartbeat via a new data
migration (`0011_seed_freshness_heartbeat.py`), added content-staleness
counts to the freshness report, documented a second Uptime Kuma keyword
monitor for paging, added a live fail-closed drill runbook
(`docs/runbooks/freshness-drill.md`), and documented the post-edit refusal
window in `docs/google-drive-next.md`.

### Current blocker: the dev stack will not currently run the drill

Checked at end of session, **before** deploying the integrated-with-fixes
branch: `celery-beat` was crash-looping and `django` had never started
(`docker ps -a` showed `django-1` as `Created`). `docker logs
knowledge-graph-infra-celery-beat-1` shows Django's own settings validator
rejecting the container's `GOOGLE_USER_OAUTH_CLIENT_SECRET_FILE` as empty or
missing:

```
django.core.exceptions.ImproperlyConfigured: GOOGLE_USER_OAUTH_CLIENT_SECRET_FILE
must name a non-empty secret file no larger than 1048576 bytes.
```

This is a container/deployment secret-mount problem, unrelated to the code
changes in this session — likely a stale container from a previous checkout
whose mounted secret file path or contents no longer resolve. It must be
fixed before any live check (freshness endpoint, the drill, or general
smoke-testing) can run. There was also no Uptime Kuma container running
(`docker ps -a` showed none); it needs to be brought up and the two monitors
from `infra/uptime-kuma/monitors.md` configured before the drill's alerting
steps can be observed.

### What the next session should do, in order

1. Diagnose and fix the `GOOGLE_USER_OAUTH_CLIENT_SECRET_FILE` mount/secret
   issue (check the compose volume mapping in `infra/compose.app.yml` against
   what actually exists at that path on the host, and whether the running
   containers are on a stale image/checkout).
2. Deploy `codex/phase-7-wp1-integrated-with-fixes` to the dev stack (`make
   up`), confirm Django, Celery worker, and Celery beat all come up healthy,
   and confirm `/api/health/freshness/` returns `200`/`"status":"ok"` with the
   new fields (`content_refresh_pending_documents`,
   `content_extraction_failed_documents`) present in the payload.
3. Bring up Uptime Kuma (`uptime-kuma` service in
   `infra/compose.infrastructure.yml`) and configure both monitors per
   `infra/uptime-kuma/monitors.md` (status-code monitor + the new
   `"status":"error"` keyword monitor), each with the
   `Authorization: Bearer <FRESHNESS_MONITOR_BEARER_KEY>` header.
4. Run the full live fail-closed drill per
   `docs/runbooks/freshness-drill.md`: stop `celery-beat`, confirm both
   monitors alert before the evidence lifetime elapses, confirm chat refuses
   at expiry, restart beat, confirm automatic recovery. Record pass/fail and
   actual timings (evidence policy: no identities, Drive IDs, or document
   text — counts and durations only).
5. On a passing drill, flip the Phase 7 tracker's `[~]` items to `[x]` in
   `ai-context/phases/phase-7-change-feed-and-evaluation.md`, journal the
   result in a new daily report entry, and only then consider WP3 (tightening
   to the 5-minute/10-minute production target) — the implementation plan
   requires WP1 to be live-validated first, not just code-complete.
6. WP2 (Drive change feed) is the work package after WP3 per the plan's
   strict sequencing; WP5 (evaluation dataset) can proceed in parallel at any
   time but is gated on the operator authoring real pilot questions, which
   cannot be automated.

## Follow-up — deployment recovery and embedded monitor removal

### What changed

- Diagnosed the failed deployment as stale app bind mounts pointing at the
  removed integration worktree; the configured Drive OAuth secret file itself
  was present, valid, and mounted read-only.
- Recreated Django and Celery from
  `codex/phase-7-wp1-integrated-with-fixes`, normalized tracked backend source
  permissions to their Git-recorded readable modes, and applied migration
  `0011_seed_freshness_heartbeat`.
- Confirmed Django and Celery worker healthy, Celery Beat scheduling the
  freshness task, and `/api/health/freshness/` returning 200/`status: ok` with
  both content-staleness fields present.
- Removed Uptime Kuma from Compose, deployment docs, and the active monitoring
  direction; removed the running container while preserving its named data
  volume for recovery.
- Replaced the product-specific monitor guide with a vendor-neutral external
  freshness-monitoring contract.
- Validation: all Compose layers render, the focused health suite passes (10
  tests), Ruff passes for the changed Python files, and `git diff --check`
  passes.

### Decisions

- Do not use Uptime Kuma. Keep the authenticated freshness endpoint and
  structured logs as vendor-neutral boundaries.
- WP1 remains open: an external alert consumer must be selected and the live
  stopped-scheduler delivery drill must pass before the 5-minute/10-minute
  target is enabled.
- Preserve the unused Uptime Kuma named volume rather than deleting monitoring
  data without separate approval.

### Next steps

- Select an external alert destination and configure the two checks documented
  in `infra/monitoring/freshness.md`.
- Run `docs/runbooks/freshness-drill.md`; only on a pass close the two `[~]`
  Phase 7 tracker items and continue the implementation-plan sequence.

## Follow-up — lean Phase 7 implementation and live closeout

### What changed

- Added a locked/retryable 15-minute Drive content reconciliation task and
  content-sync freshness age/failure fields.
- Preserved the per-user coarse content gate for unchanged successful content,
  closed it on changed/indeterminate content, and skipped non-authoritative
  `permissions.list` calls in per-user mode.
- Added the non-persistent `run_evaluation` operator command with strict private
  YAML validation, positive/citation checks, allowed/denied leak cases, and
  identity/content-free output.
- Deployed Django, Celery worker, and Celery Beat. A three-document content
  sweep succeeded with no skipped files and no unnecessary extraction; health
  returned 200/`ok` with all new content-sync fields.
- Completed the live Beat outage drill. Health changed to error 192 seconds
  after Beat stopped, roughly 26 minutes before permission evidence expired.
  At expiry the query service refused with zero citations. After Beat restart,
  scheduled content and visibility refreshes succeeded, health recovered in
  about 26 seconds, and cited retrieval recovered in about 44 seconds.
- Final validation passed: 516 backend tests, Ruff, formatting, migration
  check, Django system check, all Compose renderings, and `git diff --check`.

### Decisions

- Phase 7 is complete with the lean POC scope. Uptime Kuma, external alert
  delivery, Drive change feeds/push, 5-minute/10-minute timing, and scheduled
  evaluation remain optional Phase 9 work.
- Real client evaluation fixtures were not present. They remain ignored,
  operator-owned handoff inputs; the installed command and synthetic privacy
  tests are sufficient for code closeout.

### Next steps

- Start Phase 8 deployment handoff work.
- Create `codex/per-user-oauth-only-poc` from the Phase 7 closeout commit and
  make per-user OAuth the only documented/onboarding POC mode while leaving
  delegated ACL implementation dormant for later cleanup.
