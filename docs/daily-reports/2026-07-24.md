# 2026-07-24

- **What changed** — Added a permission-safe generic retry response for a user's own document while it is pending or running re-extraction; added direct SpiceDB/fresh-evidence tests.
- **Decisions** — The message is generic, has no citations, and never names a document or overrides authorization.
- **Next steps** — Publish the backend image, deploy it to client1, and validate the live update message during a controlled freshness drill.

- **What changed** — Corrected the retry path so it also applies when answer synthesis finds only unsupported context.
- **Decisions** — Keep the retry response generic and citation-free in every pending-content refusal path.
- **Next steps** — Publish the follow-up image, deploy it to client1, and repeat the controlled freshness drill.

- **What changed** — Passed the live client1 document-update, worker-loss
  redelivery, and stale-extraction recovery drills; recorded the acceptance
  evidence and corrected the current extraction-engine guidance.
- **Decisions** — Terminate the active Celery pool process to test
  `acks_late`/`reject_on_worker_lost` without replacing the Coolify-managed
  worker container. Use a time-compressed 61-minute stale extraction to
  exercise the real sweeper, then recover only through normal Drive
  reconciliation and requeue. Never reopen retrieval eligibility manually.
  The live client1 extraction engine was observed as `neo4j_graphrag`.
- **Next steps** — Run the several-hour soak test and private evaluation suite,
  then consolidate the remaining Phase 8 operational evidence and client
  handoff material.

- **What changed** — Ran the live private evaluation against the Owner,
  Engineer, and Finance accounts: all seven positive and allowed/denied leak
  checks passed in 19.317 seconds.
- **Decisions** — Used a temporary operator fixture inside the live Django
  container after the Coolify one-off scheduler did not dispatch tasks; both
  the fixture and its privacy-safe summary were deleted immediately after use.
- **Next steps** — Continue the 24-hour soak test, then consolidate remaining
  Phase 8 operational evidence and handoff material.

- **What changed** — Reconciled the Phase 8 tracker with the existing local
  permission-safe client demo script and its completed validation.
- **Decisions** — Treat the existing demo material as completed evidence; do
  not duplicate it while the soak test is running.
- **Next steps** — Continue the 24-hour soak test, then work through the
  remaining backup, restore, maintenance, clean-server, and handoff tasks.
